// import 'package:flutter/material.dart';
// import 'package:lab9/pages/home.dart';
// import 'package:lab9/pages/choose_location.dart';
// import 'package:lab9/pages/loading.dart';
//
// import 'quote.dart';
// import 'package:lab9/quote_card.dart';
//
// void main() => runApp(MaterialApp(
//   home: EchoList(),
// ));

// void main() => runApp(MaterialApp(
//   // home: Home(),
//     initialRoute: '/home',
//     routes: {
//       '/': (context) => Loading(),
//       '/home': (context) => Home(),
//       '/location': (context) => ChooseLocation(),
//     }
// ));

/*
class EchoList extends StatefulWidget {
  const EchoList({Key? key}) : super(key: key);

  @override
  State<EchoList> createState() => _EchoListState();
}

class _EchoListState extends State<EchoList> {
  List<Quote> quotes = [
    Quote(author: 'xyz', text: 'I see humans but no humanity'),
    Quote(text: 'The truth is realy pure and never simple', author: 'pqr'),
    Quote(
        text: 'The time is always right to do what is right',
        author: 'abc'),
  ];

      // Widget quoteTemplate(quote) {
      //   return QuoteCard(quote:quote);
      // }

/*
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.greenAccent[100],
      appBar: AppBar(
        title: Text('Quotes'),
        centerTitle: true,
        backgroundColor: Colors.lightGreen,
      ),
      body: Column(
        // children: quotes.map((quote) => quoteTemplate(quote)).toList(),
        children: quotes.map((quote) => QuoteCard(quote: quote)).toList(),
        // children: quotes.map((quote) => QuoteCard(
        //   quote: quote,
        //   delete: () {
        //     setState(() {
        //       quotes.remove(quote);
        //     });
        //   },
        // )).toList(),
      ),
    );
  }*/
}

*/


